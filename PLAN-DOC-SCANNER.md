# 📄 PLAN-DOC-SCANNER — জন্মসনদ ও ফরমের জন্য ক্যামেরা + ডকুমেন্ট স্ক্যানার

**তারিখ:** 2026-09-15 • **ভিত্তি:** `README-Document-Scanner.md` (ভিশন) + বর্তমান কোডবেস v2.1.1+19
**স্কোপ:** শুধু **BIRTH** ও **FORM** ডকুমেন্ট (PHOTO-র পাসপোর্ট পাইপলাইন অপরিবর্তিত)

---

## ১. লক্ষ্য

মোবাইল দিয়ে কাগজ তুললেই: **অটো এজ-ডিটেকশন → বাঁকা সোজা (পার্সপেক্টিভ) → ছায়ামুক্ত/পরিষ্কার →
ফিল্টার বাছাই → সঠিক ছাত্রের স্লটে সেভ** — ফটোকপির মতো ফ্রেশ ডকুমেন্ট।

---

## ২. README ভিশন ↔ বর্তমান অ্যাপ — ম্যাপিং

| README-র ধাপ | বর্তমান অবস্থা | পরিকল্পনা |
|---|---|---|
| ১. Auto Edge Detection | ❌ নেই (সাধারণ ক্যামেরা) | **ML Kit Document Scanner** — গুগলের রেডিমেড UI (এজ+অটো-ক্যাপচার বিল্ট-ইন) |
| ২. Perspective Transform | ❌ | ML Kit নিজেই করে দেয় — আলাদা OpenCV লাগবে **না** |
| ৩. Enhancement (Magic/Gray/B&W) | ❌ | `image_processor.dart`-এ ৪ মোডের ফিল্টার (isolate, pure) |
| ৪. Smart Save `281_BIRTH.jpg` | ✅ কিন্তু ভিন্ন রকম | আমাদের **v2 ফোল্ডার** (`.../282/BIRTH/282.jpg`) — README-র flat নাম **গ্রহণ করা হবে না** (এক্সপোর্ট/রিকভারি সব v2 ধরে চলে) |
| কর্নার টেনে ঠিক করা | ✅ আছে! | বিদ্যমান **ImageCropper** পাইপলাইন (ইউনিকোড-পাথ ফিক্সসহ) পুনঃব্যবহার |
| `is_doc_scanned=1` | ✅ সমতুল্য | `documents` টেবিল (UNIQUE dakhila+doc_type) |
| NID / BEFAC টাইপ | ❌ DB CHECK-এ নেই | **এই পরিকল্পনার বাইরে** (ভবিষ্যতে schema v7 হলে যোগ হবে) |

**মূল সিদ্ধান্ত:**
1. **`google_mlkit_document_scanner`** যোগ হবে — ১০ লাইনের কোডে এজ-ডিটেকশন+সোজা+ছায়ামুক্ত (README-র সুপারিশ অনুযায়ী)।
2. `edge_detection` ও `opencv_dart` **বাদ** — APK ফুলে যায় + অপ্রয়োজনীয় (ML Kit-ই করে দেয়)।
3. ম্যানুয়াল ঠিক করার জন্য নতুন কিছু নয় — **বিদ্যমান ক্রপ সিস্টেম**।

---

## ৩. গুরুত্বপূর্ণ টেকনিক্যাল সিদ্ধান্ত

| বিষয় | সিদ্ধান্ত | কারণ |
|---|---|---|
| মাল্টি-পেজ স্ক্যান | ML Kit `pageLimit: 5` → সব পেজ **উল্লম্বভাবে জোড়া** হয়ে **একটি** JPG | `documents` টেবিলে `UNIQUE(dakhila, doc_type)` — স্কিমা না ভেঙে মাল্টি-পেজ সাপোর্ট |
| ফাইল ফরম্যাট | MVP: JPEG উভয়ের জন্য | FORM পরে PDF-অপশন পাবে (Phase 3) — BIRTH-এ PDF নিষিদ্ধই থাকবে |
| ছবির সাইজ | সেভের আগে লম্বা বাহু সর্বোচ্চ **1600px** | ফাইল ছোট, ZIP/PDF হালকা; পড়ার মান অটুট |
| ইন্টারনেট | ⚠️ ম্যানিফেস্টে `INTERNET` permission **যোগ করতে হবে** | ML Kit স্ক্যানার-মডিউল প্রথমবার Play Services থেকে নামে; এরপর অফলাইনে চলে |
| Play Services না থাকলে | বার্তা + **বিদ্যমান ক্যামেরা মোডে ফলব্যাক** | Huawei ইত্যাদি ডিভাইস ভেবে |
| ইউনিকোড পাথ | স্ক্যানারের আউটপুট ক্যাশে-পাথ (ASCII) → `assignDocument` Dart-কপি v2-তে | ক্র্যাশ-ফিক্সের প্যাটার্নই |

---

## ৪. ফিল্টার — ৪টি মোড (`DocumentFilterMode`)

| মোড | কী হবে | image package বাস্তবায়ন |
|---|---|---|
| `original` | স্ক্যানারের আউটপুট যেমন তাসেম | শুধু রিসাইজ |
| `magic` (ডিফল্ট) | ব্যাকগ্রাউন্ড সাদা, লেখা গাঢ় — ছায়া কম | চ্যানেল-ভিত্তিক percentile stretch (2–98%) + contrast/brightness |
| `gray` | সাদাকালো-ধাঁচ, ফাইল ৭০% ছোট | `img.grayscale()` |
| `bw` | একদম ফটোকপি (থ্রেশহোল্ড) | Otsu global threshold (MVP); adaptive পরে |

সব ফিল্টার **isolate-এ** (UI ফ্রিজ নয়) — বিদ্যমান `image_processor.dart` প্যাটার্নেই।

---

## ৫. ইউজার ফ্লো (চূড়ান্ত)

```
ড্যাশবোর্ড → BIRTH কার্ড → [📄 স্ক্যান করুন]
→ ML Kit ফুল-স্ক্রিন স্ক্যানার (অটো এজ + অটো ক্যাপচার, ১–৫ পেজ)
→ প্রিভিউ: পেজ জোড়া → ফিল্টার ৪টির থাম্বনেইল → বাছুন
→ [✅ সেভ করুন] → v2 সেভ + গ্যালারি সিঙ্ক → ড্যাশবোর্ড "সম্পন্ন ✓"
```

---

## ৬. ফাইল-বাই-ফাইল পরিবর্তন (Phase 1 — MVP)

| ফাইল | পরিবর্তন |
|---|---|
| `pubspec.yaml` | ➕ `google_mlkit_document_scanner: ^0.4.0` |
| `android/app/src/main/AndroidManifest.xml` | ➕ `INTERNET` permission |
| `lib/utils/image_processor.dart` | ➕ `DocumentFilterMode`, `enhanceDocumentInIsolate()`, `mergePagesVerticallyInIsolate()`, `limitLongSideInIsolate()` |
| `lib/services/scanner_service.dart` (নতুন) | ML Kit র‍্যাপার: `isAvailable()`, `scan({pageLimit})` → ইমেজ-পাথ লিস্ট; এরর ম্যাপিং |
| `lib/screens/document_scanner_screen.dart` (নতুন) | স্ক্যান-পরবর্তী পাইপলাইন UI: পেজ প্রিভিউ → ফিল্টার থাম্বনেইল → সেভ |
| `lib/screens/document_dashboard_screen.dart` | BIRTH/FORM কার্ডে ➕ "স্ক্যান করুন" বাটন (প্রথম/প্রধান অ্যাকশন) |
| `test/image_processor_test.dart` | ➕ মার্জ (উচ্চতা=যোগফল), ফিল্টার (gray/bw চ্যানেল-স্ট্যাট) টেস্ট |
| `QA_CHECKLIST.md` | ➕ সেকশন O (স্ক্যানার) |

**কোর স্নিপেট (প্রত্যাশিত API):**
```dart
final result = await DocumentScanner(options: DocumentScannerOptions(
  pageLimit: 5, documentFormat: DocumentFormat.jpeg, mode: ScannerMode.full,
)).scan();
// result.images: List<XFile> → মার্জ → ফিল্টার → assignDocument-ধাঁচে সেভ
```
> ⚠️ ইনস্টলের পরে প্যাকেজের প্রকৃত API নাম (`scan()`/`scanDocument()`) মিলিয়ে নেওয়া হবে।

---

## ৭. Phase বিভাজন

| Phase | কাজ | আনুমানিক |
|---|---|---|
| **P1 — MVP** | ML Kit + মার্জ + ৪ ফিল্টার + সেভ + ফলব্যাক + টেস্ট | এক বসায় |
| **P2** | স্ক্যান-পরে "কর্নার ঠিক করুন" (ImageCropper), ফিল্টার তুলনা-ভিউ উন্নত, সেটিংসে ডিফল্ট ফিল্টার | পরের বসা |
| **P3** | FORM-এ PDF আউটপুট অপশন, ব্যাচ-স্ক্যান (একাধিক ছাত্র পরপর), NID/BEFAC (schema v7), OCR | রোডম্যাপ |

---

## ৮. রিস্ক ও মিটিগেশন

| রিস্ক | মিটিগেশন |
|---|---|
| ডিভাইসে Play Services নেই | `isAvailable()` চেক → বাংলা বার্তা + পুরনো ক্যামেরা মোডে ফলব্যাক |
| প্রথমবার ইন্টারনেট না থাকা | মডিউল-ডাউনলোড ব্যর্থ হলে একই ফলব্যাক + বার্তা |
| ৫-পেজ বিশাল স্ক্যান মেমোরি | প্রতিটি পেজ আলাদা isolate-এ প্রসেস → মার্জের সময় ধাপে ধাপে কম্পোজিট |
| স্কিমা UNIQUE ভাঙা | মার্জ-করে এক ফাইল — স্কিমা অপরিবর্তিত |
| আগের ডক হারানো | সেভ = REPLACE (বিদ্যমান নিয়ম) — ডুপ্লিকেট নয় |

---

## ৯. টেস্ট প্ল্যান

- **ইউনিট:** মার্জ-উচ্চতা = যোগফল; `gray` → RGB সমান; `bw` → শুধু ২ মান; `magic` → হিস্টোগ্রাম প্রসারিত
- **QA (সেকশন O):** ML Kit খোলা/বাতিল/ফলব্যাক, মাল্টি-পেজ, ফিল্টার প্রতিটি, BIRTH-এ PDF প্রত্যাখ্যান, ড্যাশবোর্ড স্ট্যাটাস, অফলাইনে দ্বিতীয়বার
