# 🧾 Dakhila Camera — কোড রিভিউ, বাগ/দুর্বলতা রিপোর্ট ও ভবিষ্যৎ পরিকল্পনা

**তারিখ:** 2026-09-18 • **বেস:** `master` (bc0453a) + uncommitted working tree • **তখনকার ভার্সন:** `2.1.1+26`
**স্কোপ:** `lib/` ৪৪ ফাইল (~১০.৪ হাজার লাইন), `android/` (MainActivity.kt, Manifest, Gradle), `test/` ২২ ফাইল

## ভেরিফিকেশন বেসলাইন

| চেক | ফল |
|---|---|
| `flutter analyze` | No issues found |
| `flutter test` | **86/86 পাস** (Phase 0-এর ৬টি নতুন রিগ্রেশন টেস্টসহ) |
| signed release build | `flutter build apk --release --split-per-abi` ✅ (armv7 20.2MB / arm64 22.1MB / x86_64 23.7MB) |
| APK signature | `apksigner verify` → V2 Signer, CN=Dakhila Camera (release key) |
| CI | ❌ `.github/` নেই |

## সংক্ষিপ্ত রায়

| প্রশ্ন | উত্তর |
|---|---|
| বাগ আছে? | হ্যাঁ — ১ ক্রিটিক্যাল (ডেটা লস), ৩ হাই, ৯ মিডিয়াম, ৭ লো |
| নিরাপত্তা | **keystore-এর পাসওয়ার্ড git-এ committed ছিল** (এখন ট্রি থেকে সরানো; history-তে আছে → scrub বাকি) + Auto Backup চালু, PIN দুর্বল, পাবলিক গ্যালারি কপি |
| খুব দ্রুত? | ৮০% জায়গায় হ্যাঁ; বড় ফাঁক ডকুমেন্ট-স্ক্যানার ফিল্টার/merge ও এক্সপোর্টের sync I/O-তে |
| ছাড়হীন? | Phase 0-এর পর অনেক কাছাকাছি; তবু H2 (multi-year/dakhila-key) ও M-ধাপের কাজ বাকি |
| ডায়নামিক? | ডেটা-লেভেলে হ্যাঁ (ক্লাস/ফরিক/ডক-স্লট DB থেকে); স্কিমা-লেভেলে না (৩ ডক-টাইপ + ~৩০ ফিল্ড compile-time) |
| সর্বোচ্চ কাস্টমাইজ? | আংশিক — কাস্টম ফিল্ড/ডক-টাইপ/রিপোর্ট-টেমপ্লেট নেই |

---

# Phase 0 — রিলিজ ব্লকার: ✅ সম্পন্ন (2026-09-18)

### C1. ক্লাস/ফরিক বদলালে ২০টি কলাম নীরবে মুছে যাওয়া (ডেটা লস) — ✅ ফিক্স
- **উৎস:** `lib/providers/student_provider.dart` `editStudentIdentity()` — DB-তে লেখার আগে হাতে বানানো ছোট `Student` (`toSave`) বসানো হতো; `mother_name`, `birth_date`, `birth_certificate_no`, `avg_num_*` (৪), `address_*` (৪), `stu_name_en/ar`, `father_name_en/ar`, `mother_name_en/ar` → ডিফল্ট `''`।
- **প্রমাণ (লাইভ প্রোব, তারপর মুছে ফেলা):** `BEFORE mother=মা-বেগম … AFTER mother='' | en='' | ar=''` — অথচ UI দেখাত "✅ তথ্য সংরক্ষিত হয়েছে"।
- **ফিক্স:** `old.copyWith(<সব ২৬টি এডিট-ফিল্ড updated থেকে>)` → পরে শুধু DB-মালিকানার ৩টি ফিল্ড (`imagePath/isCaptured/totalDocs`) ওভাররাইড।
- **টেস্ট:** `test/phase0_fixes_test.dart` → "C1: class/forik বদলালেও সব এডিট-ফিল্ড অক্ষত থাকে" (২০+ অ্যাসার্শন)।

### H1. "ডাটা রিসেট"-এ documents সাফ হতো না + FK cascade নিষ্ক্রিয় — ✅ ফিক্স
- `deleteAllStudents()` আগে শুধু `students` মুছত → রিসেটের পর বান্ডেল ডেটা ফিরলেও পুরনো PHOTO/BIRTH/FORM স্লট ফিরে আসত; `total_docs` stale।
- `documents → students ON DELETE CASCADE` লেখা ছিল, কিন্তু `PRAGMA foreign_keys=ON` ছাড়া কার্যকর ছিল না।
- **ফিক্স:** (১) `_initDB`-এ `onConfigure: PRAGMA foreign_keys = ON`, (২) `deleteAllStudents()` ট্রানজেকশনে `documents`+`students`, (৩) cascade চালু হওয়ায় `replaceAllStudents()`-এ **documents স্ন্যাপশট → ফিরিয়ে বসানো** (ইমপোর্টে ডক-স্লট অক্ষত, আগের আচরণ সংরক্ষিত)।
- **টেস্ট:** H1, H1b (অজানা দাখিলার ডক লেখা যায় না = FK সত্যিই চালু), H1c (ইমপোর্টে ডক টিকে থাকে)।

### H3. PHOTO ডক/ফাইল আছে কিন্তু `is_captured=0` — ✅ ফিক্স
- `_healPhotoPaths()` আগে একমুখী ছিল; `_doLoad()`-এ heal-এর **পরে** `_docs` লোড হতো → ডক-ভিত্তিক মেরামত অসম্ভব ছিল।
- **ফিক্স:** `_docs` আগে লোড + heal এখন **দুই-মুখী ও repair-only** (ফাইল পেলে রেকর্ড মেরামত; কোথাও না পেলে **কিছুই মোছে না** — storage হঠাৎ unavailable হলে ডেটা হারানোর ঝুঁকি এড়াতে)।
- **টেস্ট:** H3 (heal হয়, `captured` হিসাব ঠিক), H3b (ফাইল ছাড়া কিছু মোছে না)।

### S1. Release keystore-এর পাসওয়ার্ড git-এ — ⚠️ আংশিক সম্পন্ন
- **যা করা হয়েছে:** `PLAN-3DOC.md:196` থেকে পাসওয়ার্ড সরানো (tracked tree এখন পরিষ্কার — `git grep 'DakhilaCam@'` শুধু variable-নামে হিট দেয়); `SECURITY-KEY-ROTATION.md` রুনবুক (rotate → env-secret → history scrub → offline backup); `rotate_release_keystore.bat` (নন-ডেস্ট্রাক্টিভ রোটেশন + signed build + apksigner যাচাই); `android/app/build.gradle.kts` এখন **env-ভেরিয়েবলকে অগ্রাধিকার** দেয় (`storePassword/keyPassword/keyAlias/storeFile`), না থাকলে key.properties; `create_release_keystore.bat`-এর alias/dname/store-path আসল keystore-এর সাথে মিলিয়ে দেওয়া।
- **বাকি (আপনার সিদ্ধান্ত দরকার):** (১) আসল রোটেশন চালানো — সরাসরি APK বিতরণ হলে ইউজারকে uninstall/reinstall লাগবে, Play App Signing থাকলে upload key reset যথেষ্ট; (২) history scrub (`git filter-repo`/BFG — `SECURITY-KEY-ROTATION.md` §3), নইলে পুরনো commit `ee4b4c1`-এ পাসওয়ার্ড থেকে যাবে; (৩) `.jks`+নতুন পাসওয়ার্ডের অফলাইন ২ কপি ব্যাকআপ।

---

# Phase 1 — ডেটা নিরাপত্তা ও অখণ্ডতা: ✅ সম্পন্ন (2026-09-19)

### H2a. ইমপোর্ট collision-গার্ড (আগের ছাত্রের ছবি নতুন ছাত্রের নামে বসা) — ✅ ফিক্স
- **উৎস:** `replaceAllStudents()` — দাখিলা-মিল থাকলে `image_path`+`is_captured=1` **বছর যাচাই ছাড়াই** নতুন ইমপোর্টে বসত; একই দাখিলা-নম্বরের নতুন ব্যাচ (ভিন্ন বছর) = ভিন্ন ব্যক্তি → আগের ছাত্রের ছবি/সম্পাদনা নতুন ছাত্রের নামে বসে যেত।
- **ফিক্স:** capture/edited/name-প্রিজার্ভ ও ডক-পুনঃস্থাপন — সবই এখন **`dakhila_year|exam_year` হুবহু মিললে** কেবল; অমিলে `ReplaceReport.collisions`-এ দাখিলা যোগ হয়।
- **API:** `replaceAllStudents(maps, {preserveCaptures=true})` → `ReplaceReport{imported, collisions}` (int রিটার্ন বাদ); `importFromJsonFile`/`importFromExcelFile` রিপোর্ট প্রপাগেট করে।
- **UI:** Settings-এর ইমপোর্ট-স্ন্যাকবারে সংঘর্ষ-সতর্কতা — "N টি দাখিলার পুরনো ছবি/তথ্য বছর-অমিলের কারণে সংরক্ষিত হয়নি (দাখিলা-তালিকা…)।"
- **টেস্ট:** `test/phase1_h2_test.dart` — (১) বছর-মিলে capture প্রিজার্ভ, (২) বছর-অমিলে capture **নেয় না** + collision-এ দাখিলা, (৩) বছর-অমিলে ডক-স্লট বহন নয়।

### H2b. ধ্বংসাত্মক কাজের আগে auto-backup + দ্বিতীয় নিশ্চিতকরণ — ✅
- `_confirmReset()`: রিসেটের আগে `BackupService.backup()` (ব্যর্থ হলে রিসেট আটকায় না, শুধু জানায়) → **দ্বিতীয় AlertDialog** ("ব্যাকআপ সেভ হয়েছে: \<পথ\> / ব্যাকআপ ব্যর্থ — তবু রিসেট?") → তারপর রিসেট; স্ন্যাকবারে ব্যাকআপ-পথ।

### যাচাই
- `flutter analyze` → No issues found! • `flutter test` → **101/101** (আগে 98)।

# বাকি বাগ ও দুর্বলতা (অগ্রাধিকার অনুযায়ী)

## 🔴 হাই
| # | বিষয় | ফাইল | প্রভাব / ফিক্স |
|---|---|---|---|
| H2 | `dakhila`-ই একমাত্র key — multi-year ঝুঁকি — **আংশিক ফিক্স (Phase 1):** ইমপোর্টে capture/edited/ডক-বহন এখন `dakhila_year|exam_year` মিললেই কেবল + collision-রিপোর্ট/সতর্কতা; **বাকি:** পূর্ণ `session_id`/`academic_year` scoping + composite key (Phase 4) — কেস-নোট এখনো দাখিলা-কী | `db/database_helper.dart` (`replaceAllStudents` → `ReplaceReport`), `students.dakhila PRIMARY KEY` | ইমপোর্ট-পথে ভুল ছাত্রের ছবি বসা এখন বন্ধ; সম্পূর্ণ multi-year archive-এর জন্য স্কিমা-লেভেল স্কোপিং এখনো দরকার |
| H4 | ডকুমেন্ট-স্ক্যানার: সব পেজ এক JPEG-এ merge (৫ পেজে ≈ ৯ Mpx) + **৪টি ফিল্টারই আগেই** চালানো → ১০-৩০s CPU, isolate peak ১৫০-২৫০MB | `screens/document_scanner_screen.dart:74-128`, `utils/image_processor.dart:226-252, 306-429, 458-527` | low-end ফোনে OOM/ANR ঝুঁকি। ফিক্স: merged long-side cap (≤4000px) বা পেজ-প্রতি ফিল্টার, lazy preview (নির্বাচিত ফিল্টারই ফুল-রেজ), LUT/bulk-buffer, progress+cancel |
| SEC | Android Auto Backup চালু (`allowBackup`/`dataExtractionRules` নেই) + `requestLegacyExternalStorage="true"` + FileProvider পুরো external dir খোলা | `android/app/src/main/AndroidManifest.xml`, `res/xml/file_paths.xml` | শিশুর নাম/অভিভাবকের নম্বর/ছবি/কেস-নোট Drive auto-backup-এ যেতে পারে। ফিক্স: `allowBackup="false"` + dataExtractionRules, legacy storage বাদ, paths `Export/`,`branding/`-এ সীমিত |

## 🟠 মিডিয়াম
| # | বিষয় | ফাইল | প্রভাব / ফিক্স |
|---|---|---|---|
| M1 | UI থ্রেডে sync ডিস্ক I/O (`existsSync` হাজার ফাইলে) | `services/export_service.dart:87-91`, `providers/student_provider.dart:265-293, 744-746` | ZIP/PDF এক্সপোর্টে freeze; `await File.exists()`/directory snapshot + loop isolate-এ |
| M2 | PDF print-sheet সব পেজ মেমোরিতে | `services/export_service.dart:350-395` | পেজ-বাই-পেজ flush/streaming |
| M3 | গ্যালারির ক্যাশ-কী path/mtime দেখে না | `screens/gallery_screen.dart:46-72` | ছবি বদলালেও count একই থাকলে পুরনো ইমেজ; signature-এ path+mtime |
| M4 | গ্যালারি delete-এ `.jpg` হার্ডকোড; `updateImage`-এ mime হার্ডকোড | `student_provider.dart:605`, `database_helper.dart:568` | PNG PHOTO → orphan gallery entry / ভুল mime; `document.ext` + `mimeTypeForExt` |
| M5 | ext বের করতে `split('.').last` | `student_provider.dart:185-187, 924-926`, `database_helper.dart:562` | ফোল্ডারে ডট/এক্সটেনশনহীন পাথে ভুল ফল → অস্পষ্ট এরর; `p.extension()` |
| M6 | `CAST(dakhila AS INTEGER)` ORDER BY + `class_name`-এ index নেই | `database_helper.dart:650, 683, 717, 466-469` | লেটার/খালি দাখিলা `0` হয়ে ভুল ক্রম; বড় ডেটায় স্ক্যান |
| M8 | কেস-নোট PIN = salt-ছাড়া ৮-হেক্স FNV-1a, throttle নেই, নোট plaintext | `services/case_notes_service.dart:20-27` | brute-force সহজ; PBKDF2/HMAC+salt+delay + নোট এনক্রিপশন; UI-তে "deterrent, not encryption" লেখা |
| M9 | গ্যালারি ব্যাকআপ পাবলিক `Pictures/DakhilaCamera` | `MainActivity.kt:229-267` | জন্মসনদসহ ছবি যে-কোনো READ_MEDIA_IMAGES অ্যাপ পড়তে পারে → opt-in করুন |
## 🟡 লো
1. ডক drift: README-তে `2.1.1+17` ও "৯টি টেস্ট", প্রকৃতিতে `+26` ও ৮৬ টেস্ট; README/PLAN আপডেট বাকি।
2. CI নেই (`.github/`) — analyze+test+APK artifact pipeline বাকি।
3. `strict-casts`/`strict-inference` যোগ করা যায় (বর্তমানে ডিফল্ট `flutter_lints`)।
4. ZIP/CSV-তে বাংলা entry-নাম → Windows Explorer-এ mojibake হতে পারে (7-Zip-এ যাচাই করুন)।
5. `pubspec.yaml`-এ BOM — `increment_build_number.ps1` PowerShell 5.1-এ `-Encoding UTF8` লিখছে; `pwsh`/`utf8NoBOM` ব্যবহার করলে পরিষ্কার।
6. `cunning_document_scanner` (ML Kit) — APK সাইজ ও Play-Services নির্ভরতা (fallback আছে)।
7. ~~রিসেট/রিস্টোরে একবার confirm; auto-backup~~ — **✅ Phase 1-এ সম্পন্ন** (`_confirmReset()`: auto-backup + দ্বিতীয় AlertDialog)।

---

# ভবিষ্যৎ পরিকল্পনা (রোডম্যাপ)

**Phase 1 — ডেটা নিরাপত্তা ও অখণ্ডতা (৩-৭ দিন)**
H2 session/year scoping ডিজাইন + ইমপোর্ট collision-গার্ড; ধ্বংসাত্মক কাজের আগে auto-backup + দ্বিতীয় confirm; DB-integrity টেস্ট স্যুট (orphan, cascade, total_docs, reset semantics); Excel রাউন্ড-ট্রিপের overwrite-নীতি ডকুমেন্ট।

**Phase 2 — গতি (১-২ সপ্তাহ)**
স্ক্যানার: lazy filter প্রিভিউ, merged-cap/পেজ-প্রতি প্রসেস, progress+cancel (লক্ষ্য: ৩ পেজ < ৮s low-end); এক্সপোর্ট loop isolate-এ (`existsSync` বাদ); গ্যালারি signature-এ path+mtime; PDF sheet streaming; heal async (প্রথম পেইন্ট ব্লক না করে); perf-বাজেট টেস্ট।

**Phase 3 — নিরাপত্তা/প্রাইভেসি (২-৪ সপ্তাহ)**
`allowBackup=false` + dataExtractionRules; legacy storage বাদ; FileProvider scope সংকীর্ণ; PIN KDF+throttle + নোট এনক্রিপশন; সংবেদনশীল export-এ scope-preview confirm; গ্যালারি ব্যাকআপ opt-in; ঐচ্ছিক app-lock/DB encryption (SQLCipher)।

**Phase 4 — ডাইনামিকতা ও সর্বোচ্চ কাস্টমাইজ (১ মাস+)**
`custom_fields` (JSON) + ফর্ম-বিল্ডার; `doc_types` রেজিস্ট্রি টেবিল (CHECK enum বাদ) → কাস্টম ডক-টাইপ; রিপোর্ট-টেমপ্লেট এডিটর; সেটিংস export/import; seed-asset runtime import (ছাত্র/শিক্ষক); ZIP/CSV layout config; multi-year archive + read-only session; passport size/preset কনফিগারেবল।

**Phase 5 — QA/DevOps (চলমান)**
CI (analyze+test+signed APK/AAB); device matrix integration test (Android 9/10/13/15, 2GB RAM); perf বাজেট; offline-safe crash log; release checklist অটোমেশন; ডক sync (README/BUG_REPORT/PLAN); `flutter pub outdated` রিভিউ (৩৪ প্যাকেজ পুরনো — camera 0.10.6→0.12.1, file_picker 12→13, image 4.3→4.10 ইত্যাদি, আলাদা branch-এ যাচাই করে)।

---

# Phase 0-এর পরিবর্তন-তালিকা (ফাইল ধরে)

| ফাইল | পরিবর্তন |
|---|---|
| `lib/db/database_helper.dart` | `PRAGMA foreign_keys=ON`; `deleteAllStudents()` ট্রানজেকশনে documents+students; `replaceAllStudents()`-এ documents স্ন্যাপশট→পুনঃস্থাপন |
| `lib/providers/student_provider.dart` | C1: `old.copyWith(...)` + ৩ ফিল্ড ওভাররাইড; H3: `_docs` আগে লোড + দুই-মুখী repair-only heal |
| `test/phase0_fixes_test.dart` | **নতুন** — ৬টি রিগ্রেশন টেস্ট (C1, H1, H1b, H1c, H3, H3b) |
| `PLAN-3DOC.md` | keystore পাসওয়ার্ড সরানো |
| `SECURITY-KEY-ROTATION.md` | **নতুন** — রোটেশন/সিক্রেট/হিস্ট্রি-স্ক্রাব রুনবুক |
| `rotate_release_keystore.bat` | **নতুন** — নন-ডেস্ট্রাক্টিভ রোটেশন + signed build + verify |
| `create_release_keystore.bat` | alias/dname/store-path আসল keystore-এর সাথে মিলিয়ে দেওয়া |
| `android/app/build.gradle.kts` | env-ভেরিয়েবলকে অগ্রাধিকার (fallback: key.properties) |
| `REVIEW-2026-09-18.md` | **নতুন** — এই রিপোর্ট + রোডম্যাপ |

**তারপর যা চালানো হয়েছে:** `flutter analyze` → 0 issues • `flutter test` → 86/86 পাস • `flutter build apk --release --split-per-abi` → ৩টি signed APK • `apksigner verify` → V2 Signer (release cert) ✅

**বাকি (আপনার সিদ্ধান্ত/ম্যানুয়াল):** keystore রোটেশন চালানো + history scrub + অফলাইন ব্যাকআপ (`SECURITY-KEY-ROTATION.md`), ডিভাইসে smoke test (import → capture → ক্লাস বদল → reset → restore), ও এই working tree commit করা (Phase 1 শুরু করার আগে)।

---

# CLDR/ICU আরবী পোর্ট — ✅ সম্পন্ন (2026-09-19)

**সিদ্ধান্ত:** নাম-ট্রান্সলিটারেটরের আরবী নিয়ম-ফলব্যাক হাতের ম্যাপের বদলে **CLDR/ICU টেবিলের ডার্ট পোর্ট**
(`icu4c/source/data/translit/{Bengali_InterIndic,InterIndic_Arabic}.txt`, Unicode License — বাণিজ্যিক APK-তে নিরাপদ)।

- **নতুন ফাইল:** `lib/utils/translit/bengali_text.dart` (নরমালাইজ — একক সত্য-উৎস), `bengali_interindic.dart`
  (বাংলা→InterIndic টেবিল, ~৮২ জোড়া), `interindic_arabic.dart` (InterIndic→আরবী রুল-ইঞ্জিন: দীর্ঘতম-ম্যাচ,
  wordEnd/wordInitial/afterLetter প্রেক্ষাপট, ICU-র `$alif` সংকোচন)।
- **ইন্টিগ্রেশন:** `NameTransliterator._ruleArabic()` এখন `InterIndicArabic.transliterate()` ব্যবহার করে;
  `normalizeYaRa` → `BengaliText.normalize`। ডিকশনারি/অ্যালিয়াস/ক্যাশ অপরিবর্তিত — শুধু অজানা-টোকেনের ফলব্যাক উন্নত।
- **সব টেবিল-অক্ষর `\u` escape-এ লেখা** — এই প্রজেক্টে যে ৮-বিট এনকোডিং-দুর্ঘটনায় `normalizeYaRa`-এর লিটারেল
  নষ্ট হয়েছিল, তাতে টেবিল নিজে নষ্ট হতে পারে না।
- **মাপ (৪২৩৮ রো, ৩৩২৯ ইউনিক নাম):** আরবী আউটপুট বদল ২৬৯৮ (৮১%); পুরনো হাতের ম্যাপে **২১ নামে** En/Ar-এ
  কাঁচা বাংলা ফাঁস, **CLDR পোর্টে ০**; দুটোতেই খালি আউটপুট ০। নমুনা: `আওয়াল` → আগে `اوويال`, এখন `آوويال`
  (শব্দ-শুরুর আ → آ)। পুরনো ম্যাপের گ/پ (উর্দু-ঘেঁষা) → CLDR-এ ج/ب (আরব পাঠকের জন্য অর্থপূর্ণ)।
- **টেস্ট:** `test/name_transliterator_test.dart`-এ CLDR গোল্ডেন গ্রুপ — টেবিল-ইন্টিগ্রিটি, ব্যঞ্জন/অনুস্বার/
  ই-প্রেক্ষাপট/alif-সংকোচন, private-use ফাঁস-নিষেধ, এন্ড-টু-এন্ড; প্রত্যাশাগুলো `\u` escape-এ (ম anglিং-প্রতিরোধী,
  লাইভে প্রমাণিত — টেস্ট নিজেই একটি টাইপো ধরেছে)।
- **ভেরিফিকেশন:** `flutter analyze` → 0 issues • **`flutter test` → 96/96 পাস**।
- **সীমাবদ্ধতা:** CLDR-ল্যাটিন (ডায়াক্রিটিক-ভিত্তিক) ইচ্ছাকৃতভাবে নেওয়া হয়নি — ইংরেজি ঘরে নাম-শৈলী (Md./Hosen)
  ডিকশনারিই প্রধান, ল্যাটিন নিয়ম আগেরটাই। উর্দু-ধাঁচ চাইলে `Beng-ur` চেইন পরে যোগযোগ্য।
- **বাকি:** unknown-টোকেন এখনো ~৩২% → টপ-unknown তালিকা বের করে ডিকশনারি সম্প্রসারণ সবচেয়ে বেশি লাভ দেবে;
  প্রতিষ্ঠানভিত্তিক ওভাররাইড-ডিকশনারি (Settings → import/export) Phase 4-এ।

---

# ডিকশনারি সম্প্রসারণ — টপ-unknown টোকেন — ✅ সম্পন্ন (2026-09-19)

**পদ্ধতি:** অস্থায়ী প্রোব (পরে মুছে ফেলা) দিয়ে `assets/Data_basic.json`-এর ৪২৩৮ নামের সব টোকেন স্ক্যান →
`isKnownToken()=false` টোকেনের frequency তালিকা (UTF-8 ফাইলে, `\u`-escape সহ — কনসোলে বাংলা ম angl হওয়া এড়াতে) →
ধাপে ধাপে dict/alias যোগ → প্রতি ধাপে পুনঃস্ক্যান।

- **আগে:** ১৩৩৯ distinct / ৩১৮৪ occurrence unknown (৩২%) • **পরে:** ১০০২ distinct / ১১৯৯ occurrence (−৬২%)।
- **যোগ:** ≈২৪০ নতুন dict-এন্ট্রি + ≈৯৫ অ্যালিয়াস/রূপভেদ (মোহা:·মুহা·মোহাঃ → মোহাম্মদ; আহম্মাদ·আহম্মদ → আহমাদ;
  ত্বলহা·তলহা → তালহা; বায়জিদ·বায়েজিদ·বায়জিত → বাইজিদ; মণ্ডল → মন্ডল; ভুইয়া → ভূইয়া ইত্যাদি)।
- **ছোট কিন্তু গুরুত্বপূর্ণ ফিক্স:** লুকআপ এখন **ইউনিকোড-রূপ-নিরপেক্ষ** — `_aliases`/`_dict`-এর কী একবার
  `normalizeYaRa` চালিয়ে `_aliasesN`/`_dictN` বিল্ড হয়; কী-তে precomposed য়/ড়/ঢ় থাকলেও decomposed টোকেন
  মিস করত না (লাইভ প্রোবে `বায়েজিদ → known=false` ধরা পড়েছিল)।
- **প্রকাশ্য বাগ:** `'বায়েজিদ'`-অ্যালিয়াসের টার্গেট `'বাইজিদ'` dict-এই ছিল না (রিস্ক্যানে ৩৪ ঘটনার সেরা বাকি টোকেন) —
  dict এন্ট্রি যোগ; সাথে টেস্টে ভুল প্রত্যাশা ধরা পড়েছিল (`'ريان'.startsWith('را')` → সঠিক `'ري'`)।
- **ইচ্ছাকৃত বাদ:** `কে`, `খা` (নাম নয়) — টেস্টে must-stay-unknown হিসেবে লক।
- **টেস্ট:** `test/name_transliterator_test.dart`-এ dict/alias-শৃঙ্খল/must-unknown অ্যাসার্শন-গ্রুপ,
  `test/batch_autofill_test.dart` হালনাগাদ; **`flutter test` → ৯৮/৯৮ পাস • `flutter analyze` → 0 issues**;
  অস্থায়ী প্রোব/রিপোর্ট-ফাইল মুছে ফেলা — working tree পরিষ্কার।
- **বাকি unknown (≈১২০০ ঘটনা):** এখন ≤২ বার-দেখা টোকেনের দীর্ঘ লেজ — এক-এক করে যোগের লাভ কম; এরপরের স্তর
  প্রতিষ্ঠানভিত্তিক ওভাররাইড-ডিকশনারি (Phase 4), যাতে ইউজার নিজের বানান একবার শিখিয়ে রাখতে পারে।

# নাম-কর্পাস আপগ্রেড — ✅ সম্পন্ন (2026-09-19, assets/name.txt)

`assets/name.txt` (UTF-16LE TSV, ২১,১৮১ সারি: Bangla|English|Arabic|Gender) থেকে
`tool/build_name_corpus.dart` (ভোট-ভিত্তিক: টোকেন প্রতি সংখ্যাগরিষ্ঠ EN/AR, টপ-ভোট <৬০% শেয়ার
= conflict-স্কিপ) দিয়ে **`lib/utils/translit/name_corpus.dart` — ৭,৩৫৪ এন্ট্রি** জেনারেট করা হয়েছে।

- **লুকআপ-চেইন:** কর্পাস (২১k ভোট-প্রমাণিত) → হাতে-লেখা dict (৪৭৮) → CLDR নিয়ম-fallback।
- **মান-উন্নতি:** কর্পাস-প্রমাণিত প্রচলিত বানান বসেছে — Hossain (৮৫১ ভোট), Ayesha (২০৯), Miah,
  Mohammed, بيجوم ইত্যাদি; +২১ কর্পাস-মাইনড অ্যালিয়াস (ব্যাপারী/বছপাড়ি, মন্ডল/মণ্ডল,
  বক্বর/বক্কার, খন্দকার/খান্দকার, নূরে↔নূর ইত্যাদি)।
- **মাপ (৪২৩৮-নাম বান্ডেল ডেটাসেট):** EN ৪২৩৮/৪২৩৮ (১০০%) • AR ৪২৩৮/৪২৩৮ (১০০%) • বাংলা-ফাঁস ০।
- **যাচাই:** `flutter analyze` → No issues • `flutter test` → **98/98 পাস** • কর্পাস golden ২০/২০।
  পুরনো টেস্ট-প্রত্যাশাগুলো (Hosen/Aysha/Mia/Raiyan) কর্পাস-সত্যে হালনাগাদ।
- **রক্ষণাবেক্ষণ:** নতুন/বড় কর্পাস দিলে এক কমান্ডে পুনঃজেনারেট — `dart run tool/build_name_corpus.dart`
  (ইনপুট-যাচাই, conflict-তালিকা ও golden-সেট নিজেই তৈরি করে; বিস্তারিত ফাইল-হেডারে)।

# uCrop cancel-এ অ্যাপ-ক্র্যাশ — "Reply already submitted" — ✅ ফিক্স (2026-09-19)

**ফিল্ড ক্র্যাশ:** `RuntimeException: Failure delivering result ResultInfo{request=69, result=0,
data=null} ... IllegalStateException: Reply already submitted` — ক্রপ-এডিটর (uCrop) বাতিল করে
ফেরার সময় পুরো অ্যাপ মরে যেত।

- **নিশ্চিতকরণ (R8 mapping):** `ju` = `ImageCropperDelegate` (image_cropper 8.1.0), `eg` =
  `DartMessenger$Reply`, request 69 = `UCrop.REQUEST_CROP` — অর্থাৎ একই method-channel
  রিপ্লাই দুইবার জমা হয়ে নেটিভ দিক থেকে ছোঁড়া exception। Upstream বাগ
  ([hnvn/flutter_image_cropper#189](https://github.com/hnvn/flutter_image_cropper/issues/189),
  2020 থেকে খোলা, 12.x পর্যন্ত কোনো changelog-এ ফিক্স নেই)।
- **ফিক্স ১ (ক্র্যাশ-বন্ধ):** `MainActivity.onActivityResult` — `super.onActivityResult` এখন
  try/catch-এ; শুধু `IllegalStateException("Reply already submitted")` হলে লগ (`Log.w`) করে
  বাদ (প্রথম রিপ্লাই ডার্টে পৌঁছেই গেছে, দ্বিতীয়টা অতিরিক্ত); অন্য `IllegalStateException`
  rethrow — কিছুই লুকায় না।
- **ফিক্স ২ (প্রতিরোধ + হ্যাং-সুরক্ষা):** নতুন `lib/utils/safe_cropper.dart` (`SafeCropper.crop`)
  — একসাথে **একটিই** ক্রপ (দ্বিতীয় ডাক সঙ্গে সঙ্গে null = বাতিল) + নেটিভ রিপ্লাই **৫ মিনিট
  টাইমআউটে বাতিল** (activity recreate-এ রিপ্লাই হারালে UI আর ঝুলবে না)। তিনটি ক্রপ-কল সাইট
  (`review_screen`, `image_viewer_screen`, `document_scanner_screen`) এতে সুইচ।
- **টেস্ট:** নতুন `test/safe_cropper_test.dart` — ৬টি (path/null রিপ্লাই, single-flight,
  গার্ড-রিলিজ, টাইমআউট-বাতিল, টাইমআউট-পরবর্তী পুনঃব্যবহার, PlatformException প্রপাগেট)।
- **যাচাই:** `flutter analyze` → No issues • `flutter test` → **107/107 পাস** (আগে 101) •
  `:app:compileReleaseKotlin` → পাস। নোট: "স্ক্যানার চালু করা যায়নি" স্ক্রিনটি আলাদা ঘটনা —
  ডিভাইসে GMS ডক-স্ক্যানার না চালু হলে ফলব্যাক (বিল্ট-ইন স্ক্যানার/ক্যামেরা) পথ আছে; ক্র্যাশ
  শুধুই uCrop-ফেরতে হতো, এখন বন্ধ।


# দাখিলা-পরিচয় ফিক্স — একই দাখিলা ভিন্ন বছর = একই ব্যক্তি — ✅ সম্পন্ন (2026-09-19)

**ব্যবহারকারীর স্থির নিয়ম:** মাদ্রাসায় দাখিলা নম্বরই ছাত্রের স্থায়ী পরিচয় — একই দাখিলা
ভিন্ন বছরে = **একই ব্যক্তি** (নতুন ব্যাচ নয়)। Phase 1-এর H2 collision-গার্ড ভিন্ন-বছর
মামলাকে "ভিন্ন ব্যক্তি" ধরে ছবি/এডিট/ডক বহন আটকাত — সেটা সংশোধন।

- **`replaceAllStudents()`** (`lib/db/database_helper.dart`): capture/edited/নাম/ডক-প্রিজার্ভ
  এখন শুধু **দাখিলা-মিলে** ঘটে — `dakhila_year|exam_year` বছর-তুলনা ও
  `report.collisions` পূরণ বাদ (ক্ষেত্রটি অভ্যন্তরীণ; নতুন ব্যবহারে কখনোই ভরে না)।
  ইমপোর্টে না-থাকা দাখিলার ডক-স্লট আগের মতোই বাদ (ফাইল ডিস্কে অক্ষত)।
- **UI:** Settings-এর ইমপোর্ট-স্ন্যাকবার থেকে collision-সতর্কতা অংশ বাদ —
  এখন শুধু "N টি রেকর্ড ইমপোর্ট হয়েছে ✓"।
- **টেস্ট:** `test/phase1_h2_test.dart`-এ "বছর-অমিল = ভিন্ন ব্যক্তি" টেস্টটি উল্টে
  "একই দাখিলা, ভিন্ন বছর = **একই ব্যক্তি** — ছবি+ডক বহন হয়" হয়েছে; বাকি দুটি
  (same-year প্রিজার্ভ, reset semantics) অপরিবর্তিত।
- **সিঙ্ক-প্ল্যানে প্রভাব:** ভবিষ্যৎ Offline-First + Sync ব্যবস্থায় সার্ভার-কী এখন
  **`dakhila` একাই** (composite `(dakhila, dakhila_year)` নয়) — স্কিমা-ড্রাফট সংশোধিত।
- **যাচাই:** `flutter analyze` → No issues • `flutter test` → **107/107 পাস**।

# S1 — সার্ভার স্কিমা + REST API (XAMPP/MySQL) — ✅ তৈরি ও লাইভ-যাচাই (2026-09-19)

**প্রেক্ষাপট:** Supabase (Postgres) স্কিমা phpMyAdmin-এ চালানোয় `#1064 near 'uuid'/
'timestamptz' ... MariaDB server version` এরর — ব্যবহারকারীর সার্ভার **MariaDB 10.4 (XAMPP)**।
সিদ্ধান্ত: নিজের হোস্টিং-এ **PHP + MySQL REST API**।

- **`server/schema.sql`** (৬ টেবিল, MariaDB-যাচাইকৃত): `users`, `auth_tokens`,
  `students` (dakhila = PRIMARY KEY — একই দাখিলা ভিন্ন বছর = একই ব্যক্তি),
  `documents` (UNIQUE dakhila+doc_type, file_sha256, captured_by, is_verified),
  `case_notes`, `sync_log`। সব ইনডেক্স **টেবিলের ভেতরে** → দ্বিতীয়বার ইমপোর্টেও
  `#1061 Duplicate key` আসে না (আগে আসত — ফিক্স করা)।
- **`server/api/`** (৭ PHP ফাইল): `config.php` (MySQLi+utf8mb4, Bearer-টোকেন গার্ড),
  `login.php` (bcrypt + ৬০-দিনের টোকেন + brute-force দেরি + মেয়াদোত্তীর্ণ টোকেন ক্লিন),
  `pull.php` (`since` delta; পরের `since` = সার্ভারের `NOW()`), `push_doc.php`
  (jpeg/png, ≤১০MB, **sha256 সার্ভার নিজেই হিসাব করে**, ডুপ্লিকেটে `skipped`, নতুন ছবিতে
  আগের `is_verified` বাতিল, `is_captured/total_docs` শুধু ওই দাখিলার জন্য আপডেট),
  `download_file.php` (টোকেন + documents-এ নিবন্ধন যাচাই, path-traversal প্রতিরোধ),
  `admin_stats.php`, `create_user.php` (CLI-only)।
- **`.htaccess` ×২:** `api/`-তে Authorization হেডার পাস + আপলোড-সীমা;
  `uploads/`-এ PHP-execution বন্ধ (আপলোড-ভিত্তিক আক্রমণ প্রতিরোধ)।
- **`server/smoke-test.ps1`** — ১৭-দফা স্বয়ংক্রিয় পরীক্ষা; `server/SETUP-XAMPP.md` —
  ধাপে ধাপে গাইড + ট্রাবলশুটিং + নিরাপত্তা-চেকলিস্ট।
- **লাইভ প্রমাণ (এই পিসিতে, PHP 7.4.33 + MariaDB 10.4.27):** স্কিমা দুইবার রান → কোনো এরর নেই;
  ২টি demo ছাত্র → ২ শিক্ষক/admin → **১৭/১৭ পাস**, ছবি-আপলোড → ডাউনলোড **sha256 হুবহু মিল**;
  `since` delta-তে আপডেট হওয়া ১টি রেকর্ড ঠিক ধরা পড়ল (বাংলা অক্ষত: `টেস্ট ছাত্র আপডেট`),
  teacher-টোকেনে `admin_stats` → 403, path-traversal → 400/404। পরীক্ষার সব ডেটা/ফাইল মুছে ফেলা।
- **দুটি পুরোনো বাগ ফিক্স:** (১) PHP 8-only `str_contains/str_starts_with/str_ends_with`
  → PHP 7.4-সামঞ্জস্য `strpos/strncmp/pathinfo`; (২) ինդেক্স-ডুপ্লিকেশন।
- **পরের ধাপ (S2):** Flutter-এ লগইন + `SyncService` (pull → লোকাল DB v10, pending-ছবি →
  push_doc, অফলাইন-কিউ/retry, AppBar ব্যাজ) — `dakhila`-কী ধরে।
